# Tool/Skill 基础

## 什么是 Tool/Skill
Tool/Skill 指把外部能力（搜索、数据库查询、运行脚本、读写文件等）封装成可调用的“工具”。Agent 可以根据任务选择调用工具，再用工具结果继续完成任务。

## 设计要点
- 结构化输入输出：字段固定、类型明确，减少歧义。
- 错误可解释：失败时返回错误码与原因，便于重试或降级。
- 幂等与权限：写入类工具要避免重复执行，并做好审计。